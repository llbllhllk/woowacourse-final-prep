const string = Object.freeze({
  separator: ',',
});

const recommandMenu = Object.freeze({
  name: 0,
  menus: 1,
});

const CONSTANTS = Object.freeze({
  string,
  recommandMenu,
});

export default CONSTANTS;
